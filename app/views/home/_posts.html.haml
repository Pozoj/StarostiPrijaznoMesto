%h3 Vprašanja, predlogi in komentarji z odgovori
- for post in @posts
  .entry
    %p.asked
      = what_happened(post.senders_sex, post.post_kind_id)
      %span= post.senders_name
      dne
      = l post.original_posts_created_at
    %p.question= link_to post.posts_summary, post_path(post.posts_id)
    %p.answer= post.answers_summary
    %p.answered
      Odgovor posredoval-a:
      = link_to post.institutions_name, institution_path(post.institutions_id)
      dne
      = l post.answers_date
  %hr