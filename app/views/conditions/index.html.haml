= title "Pogoji uporabe"

- if can? :manage, Condition
  %nav#editors-resource-menu
    %ul
      = render "common/new_edit_nav", :collection => collection

- if collection.present?
  = render "common/markdown_it", :content => collection.last.content
